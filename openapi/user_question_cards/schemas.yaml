components:
  schemas:
    UserQuestionCard:
      type: object
      properties:
        question_card_id:
          type: string
          format: uuid
        sender_user_id:
          type: string
          format: uuid
        receiver_user_id:
          type: string
          format: uuid
        match_id:
          type: string
          format: uuid
        answer_text:
          type: string
          nullable: true
    UserQuestionCardResponse:
      type: object
      properties:
        user_question_card_id:
          type: string
          format: uuid
        question_card_id:
          type: string
          format: uuid
        sender_user_id:
          type: string
          format: uuid
        receiver_user_id:
          type: string
          format: uuid
        match_id:
          type: string
          format: uuid
        answer_text:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
