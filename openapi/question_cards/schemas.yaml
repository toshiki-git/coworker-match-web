components:
  schemas:
    QuestionCard:
      type: object
      properties:
        question_card_id:
          type: string
          format: uuid
        question_card_text:
          type: string
